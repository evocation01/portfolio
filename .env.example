# This file serves as an example for the required environment variables.
# Copy this file to .env.local and fill in your actual values.
# IMPORTANT: .env.local should be in your .gitignore file and never be committed.

# -- AUTHENTICATION (Auth.js / NextAuth.js) --
# A long, randomly generated string. You can generate one by running `npx auth secret` in your terminal.
AUTH_SECRET="your-super-secret-auth-secret-here"
# The canonical URL of your deployed application.
AUTH_URL="http://localhost:3000"

# -- EMAIL (Resend) --
# Your API key from Resend.
RESEND_API_KEY="re_..."

# -- NEXT.JS --
# The canonical URL of your deployed application (used for generating metadata, sitemaps, etc.).
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# -- LOGGING --
# Set to "production" for production builds, "development" otherwise.
NODE_ENV="development"

# -- ADMIN SEED SCRIPT --
# These are used ONLY for the local `pnpm db:seed` script to create your admin user.
# DO NOT add these to your Vercel environment variables.
ADMIN_EMAIL="your-admin-email@example.com"
ADMIN_PASSWORD="your-strong-password-for-seeding"

# -- DATABASE (Vercel Postgres/Neon) --
# Your main connection string from Vercel.
# NOTE: Create by pulling it directly via vercel.
POSTGRES_URL="postgres://..."
# The following are also provided by Vercel and might be used by some tools.
POSTGRES_PRISMA_URL="postgres://..."
POSTGRES_URL_NO_SSL="postgres://..."
POSTGRES_URL_NON_POOLING="postgres://..."
POSTGRES_USER="default"
POSTGRES_HOST="..."
POSTGRES_PASSWORD="..."
POSTGRES_DATABASE="verceldb"
# -- FILE UPLOADS (Vercel Blob) --
# Your Vercel Blob read/write token.
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_..."

